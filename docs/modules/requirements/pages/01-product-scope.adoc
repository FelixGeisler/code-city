= Product Scope

== M1 outcome

A user opens the approved Code City delivery target, enters one canonical URL
for an anonymously accessible public GitHub repository, and sees a
deterministic, non-configurable Code City for supported JavaScript and
TypeScript modules from one immutable repository revision.

For the input and state part of that outcome,
xref:04-functional-requirements.adoc[] is authoritative. In particular, it
defines which URL form is accepted, how the revision is selected and identified,
and when a city may be visible. For supported-source admission and the approved
M1 repository/intake limits RA-01 through RA-09,
xref:06-source-admission-and-limits.adoc[] is authoritative. For lexical SLOC,
executable-unit, complexity, canonical metric facts, and the exact
pinned-grammar acceptance boundary,
xref:08-fixed-metric-semantics.adoc[] is authoritative. For exact building
geometry and color, city content, layout and bounds, the fixed view,
interaction policy, source-free presentation boundary, passive resize, and
initial or post-`Success` construction/presentation failure,
xref:10-fixed-city-layout-and-presentation.adoc[] is authoritative.

== Included in M1

M1 includes only the smallest complete path needed for that outcome:

* one canonical GitHub repository URL as the only user-supplied input;
* anonymous use of a public repository, without user or service credentials;
* automatic selection of the GitHub-designated default branch tip and retention
  of its immutable commit identity for the attempt;
* deterministic admission and non-executing static measurement of the
  supported JavaScript/TypeScript module family with the pinned Tree-sitter
  grammars;
* one deterministic city presented through the accepted fixed browser-native
  WebGL2 mechanism, including passive resize; and
* observable initial, loading, success, failure, replacement, cancellation,
  current-context-loss, and required-resize-failure behavior with no stale or
  partial city.

The exact source-admission and approved repository/intake limit rules are in
xref:06-source-admission-and-limits.adoc[]. The exact fixed metric and grammar
rules are in xref:08-fixed-metric-semantics.adoc[]. The exact fixed city,
layout, and initial presentation rules are in
xref:10-fixed-city-layout-and-presentation.adoc[]. SP-01 through SP-04 are
unnecessary: every module and byte permitted by RA-07 through RA-09 reaches
analysis and elapsed analysis time creates no outcome. CC-09 and PR-01 are also
explicitly unnecessary: elapsed construction or presentation time alone
creates no outcome. The accepted visualization mechanism is defined by
xref:architecture:adr/0008-browser-native-webgl2-instanced-city-presentation.adoc[ADR
0008].

[[non-goals]]
== Non-goals

The following are outside M1. They are not implied future commitments.

* Authentication, private repositories, credential entry, or credential
  handling.
* Non-GitHub repositories, local directories, ZIP or archive uploads, and every
  other input form.
* Branch, tag, or commit selection, or any second input beyond the repository
  URL. Displaying the automatically selected commit identity is output, not a
  selector.
* Languages or formats outside the source-admission contract, including
  framework-specific single-file formats not admitted by that contract.
* Metric configuration, aggregate decision load, findings, or higher-level
  analysis.
* Building or source selection, search, dependency exploration, and
  history/evolution views.
* Persistence, saved projects, recent-project history, sharing, publication,
  image/model/data export, and print workflows.
* User configuration of metrics, city mapping, layout, palette, rendering, or
  resource limits.
* Compatibility with, feature parity with, or default copying/porting from Code
  City v1.

== Contract boundary

This requirements slice defines observable product behavior only. Source
admission and RA-01 through RA-09 are specified by
xref:06-source-admission-and-limits.adoc[] under
https://github.com/FelixGeisler/code-city/issues/442[#442]. Fixed metric
semantics are specified by xref:08-fixed-metric-semantics.adoc[] under
https://github.com/FelixGeisler/code-city/issues/443[#443]. Fixed city, layout,
and initial-presentation semantics are specified by
xref:10-fixed-city-layout-and-presentation.adoc[] under
https://github.com/FelixGeisler/code-city/issues/444[#444]. Provider/API
details, runtime topology and trust placement, port/component ownership,
enforcement location, and executable cancellation/cleanup mechanisms remain
outside this page. Browser-native WebGL2 and its fixed
presentation constraints are accepted in
xref:architecture:adr/0008-browser-native-webgl2-instanced-city-presentation.adoc[ADR
0008] without changing the product facts owned by this module. The accepted
static-syntax mechanism and phase lifecycle are fixed by
xref:architecture:adr/0007-static-syntax-with-pinned-tree-sitter-webassembly-grammars.adoc[ADR 0007].
Exact downstream owners are linked from the authoritative contracts and
xref:04-functional-requirements.adoc#decision-boundary[Decision boundary].
